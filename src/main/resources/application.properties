spring.datasource.url=jdbc:mysql://localhost:3306/casinokvalitet?serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Univers2016
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect
spring.jpa.hibernate.ddl-auto=create-drop
#validate: validate the schema, makes no changes to the database.
#update: update the schema by only adding the new tables/columns without modifying the existing columns
#        (without removing the persisted data from db)
#create: creates the schema, destroying previous data.
#create-drop: drop the schema when the SessionFactory is closed explicitly, typically when the application is stopped.
#             and creates a new one (removing all the persisted data from db)
#none: does nothing with the schema, makes no changes to the database